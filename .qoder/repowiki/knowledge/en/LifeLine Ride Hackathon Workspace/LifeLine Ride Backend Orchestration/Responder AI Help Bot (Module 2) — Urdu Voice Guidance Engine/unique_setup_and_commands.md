Run from repo root: `python backend/help_bot_runner.py --simulate heavy_bleeding --mode replay mockdata/helpbot/scripts/heavy_bleeding.json` for deterministic scripted runs; `--mode mic` for hands-free live loop; `--prewarm-tts` renders all scripted lines into the TTS cache to stay within free-tier rate limits (~10 requests/window); `--verify-tts` performs a spoken-Urdu round-trip (TTS -> STT) per branch and writes `mockdata/helpbot/test_runs/verify_tts.json`. Requires `GEMINI_STT_MODEL`, `GEMINI_CLASSIFIER_MODEL`, `GEMINI_TTS_MODEL`, `GEMINI_TTS_VOICE` env vars if overriding defaults.
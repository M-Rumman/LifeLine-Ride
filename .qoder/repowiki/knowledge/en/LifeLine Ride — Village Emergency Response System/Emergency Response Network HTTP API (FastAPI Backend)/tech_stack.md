Python + FastAPI 0.141.x / Starlette; Pydantic `BaseModel` as cross-module contracts; Google GenAI (Gemini) default AI provider with DashScope (SenseVoice-v1 ASR, qwen-plus/qwen-vl-max text/vision, CosyVoice TTS placeholder) selectable via `TRIAGE_AI_PROVIDER`; PostgreSQL via SQLAlchemy with connection pooling (`pool_pre_ping`, `pool_recycle=300`); secrets loaded from repo-root `.env` via `python-dotenv`.